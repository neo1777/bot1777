name: bot1777
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  auto_size_text: ^3.0.0
  binance_spot: ^0.0.2
  flutter:
    sdk: flutter
  get: ^4.6.5
  settings_ui: ^2.0.2
  sidebarx: ^0.15.0
  widget_size: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
